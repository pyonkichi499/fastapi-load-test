version: '3.8'
services:
  time-api:
    build:
      context: .
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/time"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
